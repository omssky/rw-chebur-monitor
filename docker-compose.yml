services:
  monitor:
    image: ${IMAGE:-ghcr.io/omssky/rw-chebur-monitor:latest}
    restart: unless-stopped
    env_file: .env
    volumes:
      - monitor-data:/data
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"

volumes:
  monitor-data:
